name: 'Fix Terraform State - Remove Resource Group'

# This is a ONE-TIME workflow to fix the Terraform state
# It removes the resource group from state (which is now a data source)
# After running this successfully, you can delete this workflow file

on:
  workflow_dispatch:
    inputs:
      environment:
        description: 'Environment (dev/staging/prod)'
        required: true
        type: choice
        options:
          - dev
          - staging
          - prod
        default: dev
      confirm:
        description: 'Type "REMOVE" to confirm state removal'
        required: true
        type: string

env:
  TERRAFORM_VERSION: '1.11.4'

jobs:
  remove-resource-group-from-state:
    name: 'Remove Resource Group from State'
    runs-on: ubuntu-latest
    
    permissions:
      id-token: write
      contents: read

    defaults:
      run:
        shell: bash
        working-directory: platform/infra/foundation

    steps:
      - name: Validate Confirmation
        run: |
          if [ "${{ inputs.confirm }}" != "REMOVE" ]; then
            echo "❌ Confirmation failed. You must type 'REMOVE' to proceed."
            exit 1
          fi
          echo "✅ Confirmation validated"

      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TERRAFORM_VERSION }}
          terraform_wrapper: false

      - name: Terraform Init
        run: terraform init -input=false
        env:
          ARM_USE_OIDC: true
          ARM_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
          ARM_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}
          ARM_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: List Current State
        run: |
          echo "📋 Current Terraform state:"
          terraform state list
        env:
          ARM_USE_OIDC: true
          ARM_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
          ARM_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}
          ARM_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Remove Resource Group from State
        run: |
          echo "🗑️ Removing azurerm_resource_group.riskinsure from state..."
          
          # Check if the resource exists in state before trying to remove
          if terraform state list | grep -q "azurerm_resource_group.riskinsure"; then
            terraform state rm azurerm_resource_group.riskinsure
            echo "✅ Successfully removed azurerm_resource_group.riskinsure from state"
          else
            echo "ℹ️ Resource azurerm_resource_group.riskinsure not found in state (already removed or never existed)"
          fi
        env:
          ARM_USE_OIDC: true
          ARM_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
          ARM_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}
          ARM_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Verify State After Removal
        run: |
          echo "📋 Updated Terraform state:"
          terraform state list
          
          # Verify the resource is gone
          if terraform state list | grep -q "azurerm_resource_group.riskinsure"; then
            echo "❌ ERROR: Resource still exists in state!"
            exit 1
          else
            echo "✅ Verified: Resource successfully removed from state"
          fi
        env:
          ARM_USE_OIDC: true
          ARM_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
          ARM_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}
          ARM_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Summary
        run: |
          echo "## ✅ State Fix Complete" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "The resource group has been removed from Terraform state." >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Next Steps:**" >> $GITHUB_STEP_SUMMARY
          echo "1. Run your normal terraform plan/apply workflow" >> $GITHUB_STEP_SUMMARY
          echo "2. Verify the deployment succeeds" >> $GITHUB_STEP_SUMMARY
          echo "3. Delete this workflow file (.github/workflows/fix-terraform-state.yaml)" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**What happened:**" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ Resource group remains in Azure (unchanged)" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ Terraform now treats it as a data source (read-only)" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ No resources were deleted from Azure" >> $GITHUB_STEP_SUMMARY
