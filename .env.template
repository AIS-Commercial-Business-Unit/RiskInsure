# ============================================================================
# RiskInsure Docker Compose Environment Variables
# ============================================================================
# Copy this file to .env and fill in the values
# DO NOT commit .env to source control

# Use emulator defaults (leave empty to use emulator connection strings)
COSMOSDB_CONNECTION_STRING=
SERVICEBUS_CONNECTION_STRING=

# OR use Azure services (for testing against real Azure resources)
# COSMOSDB_CONNECTION_STRING=AccountEndpoint=https://YOUR-COSMOS.documents.azure.com:443/;AccountKey=YOUR-KEY==
# SERVICEBUS_CONNECTION_STRING=Endpoint=sb://YOUR-NAMESPACE.servicebus.windows.net/;SharedAccessKeyName=RootManageSharedAccessKey;SharedAccessKey=YOUR-KEY=
